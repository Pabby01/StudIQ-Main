import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function MarketTable(_props?: any) {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Markets</CardTitle>
      </CardHeader>
      <CardContent>
        <div className="text-sm text-gray-600">Market data table is coming soon.</div>
      </CardContent>
    </Card>
  );
}

export default MarketTable;
